import { EmployeeInviteEntity } from './employee-invite.entity';

describe('EmployeeInviteEntity', () => {
  it('should be defined', () => {
    expect(new EmployeeInviteEntity()).toBeDefined();
  });
});

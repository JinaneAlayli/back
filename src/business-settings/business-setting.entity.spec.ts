import { BusinessSettingEntity } from './business-setting.entity';

describe('BusinessSettingEntity', () => {
  it('should be defined', () => {
    expect(new BusinessSettingEntity()).toBeDefined();
  });
});

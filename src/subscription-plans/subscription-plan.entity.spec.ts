import { SubscriptionPlanEntity } from './subscription-plan.entity';

describe('SubscriptionPlanEntity', () => {
  it('should be defined', () => {
    expect(new SubscriptionPlanEntity()).toBeDefined();
  });
});
